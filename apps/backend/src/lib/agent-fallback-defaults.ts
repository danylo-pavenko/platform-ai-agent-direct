/** Default canned customer fallbacks (uk + en). Shared by agent-config and agent-fallback. */

export const CUSTOMER_FALLBACK_BUSY =
  'Дякуємо за повідомлення! Менеджер відпише трохи пізніше.';

export const CUSTOMER_FALLBACK_BUSY_EN =
  'Thanks for your message! A manager will reply a bit later.';

export const CUSTOMER_FALLBACK_TIMEOUT =
  'Одну хвилинку, менеджер відпише трохи пізніше.';

export const CUSTOMER_FALLBACK_TIMEOUT_EN =
  'One moment — a manager will get back to you shortly.';
